<UserControl x:Class="JudgeSharp.Views.InputBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JudgeSharp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Name="HeaderText" Margin="5" Foreground="White"/>
        <TextBox Name="ContentTextBox" Grid.Row="1" Margin="5" Background="#FF3E3E42" Foreground="White" AcceptsReturn="True" AcceptsTab="True" />
        <Button Name="OKButton" IsDefault="True" Content="OK" Margin="0,3,3,3" Grid.Row="2" HorizontalAlignment="Right" Width="60"/>
        <Button Name="CancelButton" IsCancel="True" Content="Cancel" Margin="0,3,68,3" Grid.Row="2" HorizontalAlignment="Right" Width="60"/>
    </Grid>
</UserControl>
